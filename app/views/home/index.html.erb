<main class="wrapper home-index__wrapper">
  <%= image_tag("logo.svg", :class => "home-index__logo") %>
  <% if current_user.present? %>
    <%= link_to 'Déconnexion', destroy_user_authentication_path, class: 'button home-index__action' %>
  <% else %>
    <%= link_to 'Connexion', new_user_authentication_path, class: 'button button--block home-index__action' %>
    <%= link_to 'Inscription', new_user_path, class: 'button button--block home-index__action' %>
  <% end %>
</main>
